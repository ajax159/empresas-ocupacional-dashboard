import React from 'react';
import PlaceholderPage from '../components/PlaceholderPage';

const AboutPage: React.FC = () => {
    return (
        <PlaceholderPage
            title="Acerca de"
            description="Información sobre el sistema de gestión de salud ocupacional, versión y desarrolladores."
        />
    );
};

export default AboutPage;
